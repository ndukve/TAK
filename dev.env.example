# Local dev/preview environment — admin panel only, no real TAK server.
# Copy is generated automatically by `./dev.sh up` as `dev.env` (gitignored).
# This file documents the values; it is never read directly.

POSTGRES_ADDRESS=localhost
POSTGRES_USER=devuser
POSTGRES_PASSWORD=devpass
ADMIN_SECRET_KEY=dev-only-not-for-production-use-123456789
ADMIN_FIRST_USER=admin
ADMIN_FIRST_PASS=devpass123
ADMIN_HIBP_CHECK=0
